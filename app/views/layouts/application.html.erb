<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="description" content="">
  <meta name="author" content="">
  <title><%= full_title(yield(:title)) %></title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <!-- Bootstrap Core CSS - Uses Bootswatch Flatly Theme: http://bootswatch.com/flatly/ -->
  <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css" rel="stylesheet">
  <link href="http://maxcdn.bootstrapcdn.com/font-awesome/4.4.0/css/font-awesome.min.css" rel="stylesheet">
  <link href="http://fonts.googleapis.com/css?family=Montserrat:400,700" rel="stylesheet" type="text/css">
  <link href="http://fonts.googleapis.com/css?family=Lato:400,700,400italic,700italic" rel="stylesheet" type="text/css">
  <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
  <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
  <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
    <script src="https://oss.maxcdn.com/libs/respond.js/1.4.2/respond.min.js"></script>
  <![endif]-->
</head>
<body id="page-top" class="index">
  <div class="row-fluid">
    <% flash.each do |key, value| %>
      <%= content_tag(:div, value, class: "alert alert-#{key}") %>
    <% end %>
  </div>
    <%= render 'layouts/header' %>
    <!-- Job Grid Section -->
    <section id="job">
      <div class="container">
        <div class="row">
          <%= yield %>
        </div>
      </div>
    </section>

    <!-- Footer -->
    <footer class="text-center">
        <div class="footer-above">
            <div class="container">
                <div class="row">
                    <div class="footer-col col-md-4">
                      <h3>Location</h3>
                      <% if current_page?(companies_path) %>
                        <p><%= Company.first.description %><br><%= Company.first.address %></p>
                      <% elsif current_page?(new_company_path) %>
                        <p>Company name<br>Address, City</p>
                      <% elsif @company.present? && !(current_page?(new_company_path)) %>
                        <p><%= @company.description %><br><%= @company.address %></p>
                      <% end %>
                    </div>
                    <div class="footer-col col-md-4">
                        <h3>Around the Web</h3>
                        <ul class="list-inline">
                            <li>
                              <% if current_page?(companies_path) %>
                                <a href="<%= Company.first.facebook %>" class="btn-social btn-outline"
                                  target="_blank"><i class="fa fa-fw fa-facebook"></i>
                                </a>
                              <% elsif current_page?(new_company_path) %>
                                <a href="#" class="btn-social btn-outline">
                                  <i class="fa fa-fw fa-facebook"></i>
                                </a>
                              <% elsif @company.present? && !(current_page?(new_company_path)) %>
                                <a href="<%= @company.facebook %>" class="btn-social btn-outline"
                                  target="_blank"><i class="fa fa-fw fa-facebook"></i>
                                </a>
                              <% end %>
                            </li>
                            <li>
                              <% if current_page?(companies_path) %>
                                <a href="<%= Company.first.google_plus %>" class="btn-social btn-outline" target="_blank"><i class="fa fa-fw fa-google-plus"></i>
                                </a>
                              <% elsif current_page?(new_company_path) %>
                                <a href="#" class="btn-social btn-outline">
                                  <i class="fa fa-fw fa-google-plus"></i>
                                </a>
                              <% elsif @company.present? && !(current_page?(new_company_path)) %>
                                <a href="<%= @company.google_plus %>" class="btn-social btn-outline"
                                  target="_blank"><i class="fa fa-fw fa-google-plus"></i>
                                </a>
                              <% end %>
                            </li>
                            <li>
                              <% if current_page?(companies_path) %>
                                <a href="<%= Company.first.twitter %>" class="btn-social btn-outline"
                                  target="_blank"><i class="fa fa-fw fa-twitter"></i>
                                </a>
                              <% elsif current_page?(new_company_path) %>
                                <a href="#" class="btn-social btn-outline">
                                  <i class="fa fa-fw fa-twitter"></i>
                                </a>
                              <% elsif @company.present? && !(current_page?(new_company_path)) %>
                                <a href="<%= @company.twitter %>" class="btn-social btn-outline"
                                  target="_blank"><i class="fa fa-fw fa-twitter"></i>
                                </a>
                              <% end %>
                            </li>
                            <li>
                              <% if current_page?(companies_path) %>
                                <a href="<%= Company.first.linkedin %>" class="btn-social btn-outline"
                                  target="_blank"><i class="fa fa-fw fa-linkedin"></i>
                                </a>
                              <% elsif current_page?(new_company_path) %>
                                <a href="#" class="btn-social btn-outline">
                                  <i class="fa fa-fw fa-linkedin"></i>
                                </a>
                              <% elsif @company.present? && !(current_page?(new_company_path)) %>
                                <a href="<%= @company.linkedin %>" class="btn-social btn-outline"
                                  target="_blank"><i class="fa fa-fw fa-linkedin"></i>
                                </a>
                              <% end %>
                            </li>
                            <li>
                              <% if current_page?(companies_path) %>
                                <a href="<%= Company.first.dribble %>" class="btn-social btn-outline"
                                  target="_blank"><i class="fa fa-fw fa-dribbble"></i>
                                </a>
                              <% elsif current_page?(new_company_path) %>
                                <a href="#" class="btn-social btn-outline">
                                  <i class="fa fa-fw fa-dribbble"></i>
                                </a>
                              <% elsif @company.present? && !(current_page?(new_company_path)) %>
                                <a href="<%= @company.dribble %>" class="btn-social btn-outline"
                                  target="_blank"><i class="fa fa-fw fa-dribbble"></i>
                                </a>
                              <% end %>
                            </li>
                        </ul>
                    </div>
                    <div class="footer-col col-md-4">
                        <h3>About Furaha</h3>
                        <p>Furaha Software Inc is one of the best software development company in the planet, For more visit at <a href="http://www.furahasoftware.com/">Furaha Web</a></p>
                    </div>
                </div>
            </div>
        </div>
        <div class="footer-below">
            <div class="container">
                <div class="row">
                    <div class="col-lg-12">
                        Copyright &copy; 
                        <% if current_page?(companies_path) %>
                          <%= Company.first.name %>
                        <% elsif current_page?(new_company_path) %>
                          <%= 'Company' %>
                        <% elsif @company.present? && !(current_page?(new_company_path)) %>
                          <%= @company.name %>
                        <% end %>
                        <%= Date.today.strftime("%Y") %>
                    </div>
                </div>
            </div>
        </div>
    </footer>

    <!-- Scroll to Top Button (Only visible on small and extra-small screen sizes) -->
    <div class="scroll-top page-scroll visible-xs visible-sm">
        <a class="btn btn-primary" href="#page-top">
            <i class="fa fa-chevron-up"></i>
        </a>
    </div>

    <!-- job Modals -->

    <!-- jQuery -->
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1/jquery.min.js"></script>
    <script src="http://code.jquery.com/ui/1.11.1/jquery-ui.min.js"></script> 

    <!-- Bootstrap Core JavaScript -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>

    <!-- Plugin JavaScript -->
    <script src="http://cdnjs.cloudflare.com/ajax/libs/jquery-easing/1.3/jquery.easing.min.js"></script>
</body>
</html>